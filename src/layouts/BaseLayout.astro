---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SearchModal from '../components/SearchModal.astro';
import CodeCopyButton from '../components/CodeCopyButton.astro';
import { siteConfig } from '../config/site.config';
import type { Lang } from '../i18n/utils';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  lang?: Lang;
  article?: boolean;
}

const {
  lang = 'id',
  title,
  description,
  image = siteConfig.avatar,
  article = false
} = Astro.props;

const pageTitle = title
  ? `${title} | ${siteConfig.title[lang]}`
  : `${siteConfig.title[lang]} - ${siteConfig.eyebrow[lang]}`;
const pageDescription = description || siteConfig.description[lang];
const canonicalURL = new URL(Astro.url.pathname, Astro.site || siteConfig.siteUrl);
const ogImage = new URL(image, Astro.site || siteConfig.siteUrl);
---

<!doctype html>
<html lang={lang === 'id' ? 'id-ID' : 'en-US'} class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Fonts matching labs.muhfaridzia.live -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600;9..40,700&family=Playfair+Display:ital,wght@0,500;0,600;0,700;1,500;1,600;1,700&display=swap"
      rel="stylesheet"
    />

    <!-- Favicon using Circular User Photo -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" href="/favicon.png" />

    <!-- Primary Meta Tags -->
    <title>{pageTitle}</title>
    <meta name="title" content={pageTitle} />
    <meta name="description" content={pageDescription} />
    <meta name="author" content={siteConfig.author} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content={article ? 'article' : 'website'} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:site_name" content={siteConfig.title[lang]} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={pageTitle} />
    <meta property="twitter:description" content={pageDescription} />
    <meta property="twitter:image" content={ogImage} />

    <!-- RSS Feed link -->
    <link
      rel="alternate"
      type="application/rss+xml"
      title={siteConfig.title[lang]}
      href={lang === 'id' ? '/id/rss.xml' : '/en/rss.xml'}
    />

    <!-- Anti-flicker Dark Mode Script -->
    <script is:inline>
      (function () {
        const theme = localStorage.getItem('theme');
        const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (theme === 'dark' || (!theme && systemDark)) {
          document.documentElement.classList.add('dark');
        } else {
          document.documentElement.classList.remove('dark');
        }
      })();
    </script>
  </head>
  <body class="min-h-screen flex flex-col selection:bg-[var(--lime)] selection:text-[#17221c] overflow-x-hidden max-w-full">
    <Header lang={lang} />
    
    <main id="main-content" class="flex-1 w-full max-w-[1120px] mx-auto px-4 sm:px-6 md:px-8 py-6 sm:py-8 md:py-12 min-w-0">
      <slot />
    </main>

    <Footer lang={lang} />
    <SearchModal lang={lang} />
    <CodeCopyButton lang={lang} />
  </body>
</html>
