---
import { siteConfig } from '../config/site.config';
import type { Lang } from '../i18n/utils';
import { useTranslations, formatUrl } from '../i18n/utils';
import ThemeToggle from './ThemeToggle.astro';
import LanguageSwitcher from './LanguageSwitcher.astro';
import Icon from './Icon.astro';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
const title = siteConfig.title[lang];
const archiveUrl = formatUrl('/#archive', lang);
const aboutUrl = formatUrl('/about', lang);
const rssUrl = lang === 'id' ? '/id/rss.xml' : '/en/rss.xml';
---

<header class="sticky top-0 z-40 w-full border-b border-[var(--line)] bg-[var(--paper)]/95 backdrop-blur-md transition-colors">
  <div class="max-w-[1120px] mx-auto px-4 sm:px-6 md:px-8 h-18 flex items-center justify-between">
    <!-- Brand matching labs.muhfaridzia.live -->
    <a href={formatUrl('/', lang)} class="flex items-center gap-3 group focus-visible:outline-2 focus-visible:outline-[var(--green)] rounded-lg">
      <img
        src={siteConfig.avatar}
        alt={siteConfig.author}
        class="w-10 h-10 rounded-full object-cover border border-[var(--line)] group-hover:scale-105 transition-transform"
        width="40"
        height="40"
      />
      <span class="font-semibold text-[var(--ink)] tracking-tight text-lg group-hover:text-[var(--green)] transition-colors">
        {title}
      </span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="hidden md:flex items-center gap-6 text-sm font-medium text-[var(--ink-soft)]">
      <a href={archiveUrl} class="hover:text-[var(--green)] transition-colors">
        {t('nav.archive')}
      </a>
      <a href={aboutUrl} class="hover:text-[var(--green)] transition-colors">
        {t('nav.about')}
      </a>
      <a href={rssUrl} target="_blank" class="hover:text-[var(--green)] transition-colors flex items-center gap-1.5" title="RSS Feed">
        <Icon name="rss" class="w-3.5 h-3.5" />
        <span>{t('nav.rss')}</span>
      </a>

      <!-- Search Trigger -->
      <button
        type="button"
        id="search-trigger-btn"
        class="flex items-center gap-2 px-3 py-1.5 rounded-full border border-[var(--line)] text-xs text-[var(--muted)] hover:text-[var(--ink)] hover:border-[var(--green)] bg-[var(--card)] transition-colors cursor-pointer"
        aria-label={t('search.button')}
      >
        <Icon name="search" class="w-3.5 h-3.5" />
        <span>{t('search.placeholder')}</span>
        <kbd class="ml-1 px-1.5 py-0.5 text-[10px] font-mono bg-[var(--paper-deep)] border border-[var(--line)] rounded-sm">⌘K</kbd>
      </button>

      <div class="h-4 w-px bg-[var(--line)]" />

      <!-- Language & Theme -->
      <LanguageSwitcher currentLang={lang} />
      <ThemeToggle />
    </nav>

    <!-- Mobile Controls -->
    <div class="flex items-center gap-2 md:hidden">
      <button
        type="button"
        id="search-trigger-mobile"
        class="p-2 rounded-full text-[var(--ink-soft)] hover:text-[var(--green)] border border-[var(--line)] bg-[var(--card)] cursor-pointer"
        aria-label={t('search.button')}
      >
        <Icon name="search" class="w-4 h-4" />
      </button>
      <LanguageSwitcher currentLang={lang} />
      <ThemeToggle />
      <button
        type="button"
        id="mobile-menu-btn"
        class="p-2 rounded-full text-[var(--ink-soft)] hover:text-[var(--green)] border border-[var(--line)] bg-[var(--card)] cursor-pointer"
        aria-label="Toggle navigation menu"
        aria-expanded="false"
      >
        <span id="mobile-menu-icon-open">
          <Icon name="menu" class="w-4 h-4" />
        </span>
        <span id="mobile-menu-icon-close" class="hidden">
          <Icon name="close" class="w-4 h-4" />
        </span>
      </button>
    </div>
  </div>

  <!-- Mobile Dropdown Menu -->
  <div id="mobile-menu" class="hidden md:hidden border-b border-[var(--line)] bg-[var(--paper)] px-6 py-4 space-y-3 text-sm font-medium">
    <a href={archiveUrl} class="block py-1.5 text-[var(--ink-soft)] hover:text-[var(--green)]">
      {t('nav.archive')}
    </a>
    <a href={aboutUrl} class="block py-1.5 text-[var(--ink-soft)] hover:text-[var(--green)]">
      {t('nav.about')}
    </a>
    <a href={rssUrl} target="_blank" class="flex items-center gap-2 py-1.5 text-[var(--ink-soft)] hover:text-[var(--green)]">
      <Icon name="rss" class="w-4 h-4" />
      <span>{t('nav.rss')} Feed</span>
    </a>
  </div>
</header>

<script>
  function setupMobileMenu() {
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');
    const iconOpen = document.getElementById('mobile-menu-icon-open');
    const iconClose = document.getElementById('mobile-menu-icon-close');

    if (!btn || !menu) return;

    btn.addEventListener('click', () => {
      const isExpanded = btn.getAttribute('aria-expanded') === 'true';
      btn.setAttribute('aria-expanded', (!isExpanded).toString());
      menu.classList.toggle('hidden');
      iconOpen?.classList.toggle('hidden');
      iconClose?.classList.toggle('hidden');
    });
  }

  setupMobileMenu();
  document.addEventListener('astro:after-swap', setupMobileMenu);
</script>
